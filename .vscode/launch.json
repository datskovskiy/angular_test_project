{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Launch Chrome (default)",
        "type": "chrome",
        "userDataDir": "${workspaceFolder}/.vscode/chrome",
        "request": "launch",
        "url": "http://localhost:4200/#",
        "webRoot": "${workspaceFolder}",
        "skipFiles": [
          "./node_modules/**"
        ]
      }
    ]
  }